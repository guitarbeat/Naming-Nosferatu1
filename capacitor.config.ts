import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.namingnosferatu',
  appName: 'Naming-Nosferatu',
  webDir: 'dist'
};

export default config;
